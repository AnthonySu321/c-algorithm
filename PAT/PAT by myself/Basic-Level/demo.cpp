#include<stdio.h>
#include<vector>
#include<iostream>

using namespace std;
int main()
{
    // vector<int> nums;
    // nums = vector<int> (3);
    vector<int> num(size, 1);
    int l = num.size();
    cout<<l<<endl;//Êä³öÎª3
}
